<VirtualHost *:80>
    ServerName localhost

    # PHP配置
    DocumentRoot /app/public
    <Directory /app/public>
        AllowOverride All
        Require all granted
    </Directory>

    # Flask配置
    WSGIDaemonProcess flaskapp threads=5
    WSGIScriptAlias /flask /app/flaskapp.wsgi

    <Directory /app>
        Require all granted
    </Directory目录到系统路径
sys.path.insert(0, os.path.dirname(__file__))

from app import app as application  # 假设你的 Flask 应用在 app.py 中
